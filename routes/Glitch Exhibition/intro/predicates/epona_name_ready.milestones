milestones 1.7

milestone epona_name_ready {
  phase pre_input
  when menu.name_scene.present && menu.name_scene.procedure == 1 &&
   menu.file_select.present && menu.file_select.procedure == 19 &&
   menu.name_entry.active && menu.name_entry.selection_procedure == 8
}
